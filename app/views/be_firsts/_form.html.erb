<%= form_with(model: be_first, local: true) do |form| %>
  <% if be_first.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(be_first.errors.count, "error") %> prohibited this be_first from being saved:</h2>

      <ul>
      <% be_first.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :be_first_name %>
  </div>

  <div class="field">
    <%= form.label :phone_number %>
    <%= form.text_field :phone_number, id: :be_first_phone_number %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :be_first_email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
