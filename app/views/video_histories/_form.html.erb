<%= form_with(model: video_history, local: true) do |form| %>
  <% if video_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(video_history.errors.count, "error") %> prohibited this video_history from being saved:</h2>

      <ul>
      <% video_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :video_history_user_id %>
  </div>

  <div class="field">
    <%= form.label :video_id %>
    <%= form.text_field :video_id, id: :video_history_video_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
