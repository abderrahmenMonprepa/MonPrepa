<%= form_with(model: user_matter, local: true) do |form| %>
  <% if user_matter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_matter.errors.count, "error") %> prohibited this user_matter from being saved:</h2>

      <ul>
      <% user_matter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :section %>
    <%= form.text_field :section, id: :user_matter_section %>
  </div>

  <div class="field">
    <%= form.label :level %>
    <%= form.text_field :level, id: :user_matter_level %>
  </div>

  <div class="field">
    <%= form.label :matter %>
    <%= form.text_field :matter, id: :user_matter_matter %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
