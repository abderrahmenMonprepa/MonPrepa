<%= form_with(model: document_favori, local: true) do |form| %>
  <% if document_favori.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(document_favori.errors.count, "error") %> prohibited this document_favori from being saved:</h2>

      <ul>
      <% document_favori.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :document_favori_user_id %>
  </div>

  <div class="field">
    <%= form.label :document_id %>
    <%= form.text_field :document_id, id: :document_favori_document_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
